<div class="row justify-content-center">
    {{#if flashMessages}}
        {{#if flashMessages.success}}
            <span class="text-success">{{{flashMessages.success}}}</span>
        {{else}}
            <span clas="text-danger">{{{flashMessages.error}}}</span>
        {{/if}}
    {{/if}}
</div>